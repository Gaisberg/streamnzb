streamnzb.exe
streamnzb.exe~
.env
.git
dist/
config.json
pkg/server/web/static
state.json
*.log
.augment
streamnzb.db*
releasepipeline
.agents
# Local Claude Code state (settings, launch config) stays out; the skills
# directory is shared so every contributor's assistant gets the same
# architecture and workflow guidance. See CONTRIBUTING.md.
.claude/*
!.claude/skills/

postgres_data

# Downloaded ffprobe binaries (populated by `go run ./tools/fetch-ffprobe` at build time)
/ffprobe
/ffprobe.exe
pkg/media/ffprobe/bin/ffprobe-*

# Review backlog: a working record, deliberately not part of the repository.
/backlog.md

streamnzb
